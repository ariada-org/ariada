// Тесты собираются и рядом с исходником, и в отдельном каталоге.
//
// ОТКУДА. Настройка собирала только `tests/**`. Сторож на очистку разметки лежал
// в `src/` рядом с тем, что стережёт, — и не запускался ни разу. Он выглядел
// покрытием: файл на месте, имя говорящее, содержимое верное. Снятие правки его
// не роняло, потому что ронять было нечего.
//
// Тест, который никто не гоняет, и тест, который проходит, издали одного цвета.
export default { test: { include: ['src/**/*.test.ts', 'tests/**/*.test.ts'] } };
