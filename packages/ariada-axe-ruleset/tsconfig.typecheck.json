{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true,
    "declaration": false,
    "declarationMap": false,
    "composite": false,
    "isolatedDeclarations": false,
    "allowImportingTsExtensions": true
  },
  "//": "exclude is restated on purpose: it is inherited from the base config and beats the include below, so naming the tests there without this reads none of them and still reports success.",
  "exclude": ["node_modules", "dist"],
  "include": ["src/**/*.ts", "tests/**/*.ts", "vitest.config.ts"]
}
