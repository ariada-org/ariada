{
  "domain": "structured-data",
  "rule": "sd-product-required",
  "standard": ["Schema.org Product — Google Rich Results required fields (schema.org/Product)"],
  "cases": [
    {
      "id": "fail-1",
      "selector": "script[type=\"application/ld+json\"]:nth-of-type(1)",
      "expect": "fail",
      "severity": "serious",
      "why": "Product block has name and image but no description; PRODUCT_REQUIRED_FIELDS check emits one serious finding for the missing 'description' property"
    },
    {
      "id": "fail-2",
      "selector": "script[type=\"application/ld+json\"]:nth-of-type(2)",
      "expect": "fail",
      "severity": "serious",
      "why": "Product block is missing image, description, and the entire offers object; three serious findings are emitted (one per missing field: image, description, offers)"
    },
    {
      "id": "pass-1",
      "selector": "script[type=\"application/ld+json\"]:nth-of-type(3)",
      "expect": "pass"
    }
  ],
  "alsoExpect": []
}
