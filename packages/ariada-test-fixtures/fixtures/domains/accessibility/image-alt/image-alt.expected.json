{
  "domain": "accessibility",
  "rule": "image-alt",
  "standard": ["WCAG 2.2 SC 1.1.1", "EN 301 549 9.1.1.1"],
  "cases": [
    {
      "id": "fail-1",
      "selector": "#fail-1",
      "expect": "fail",
      "severity": "serious",
      "why": "img has no alt attribute; hasAltText returns false because attributes has no 'alt' key"
    },
    {
      "id": "fail-2",
      "selector": "#fail-2",
      "expect": "fail",
      "severity": "serious",
      "why": "img has alt=\"\" (empty string); hasAltText returns false because value.trim().length === 0"
    },
    {
      "id": "pass-1",
      "selector": "#pass-1",
      "expect": "pass"
    },
    {
      "id": "pass-2",
      "selector": "#pass-2",
      "expect": "pass"
    }
  ],
  "alsoExpect": []
}
