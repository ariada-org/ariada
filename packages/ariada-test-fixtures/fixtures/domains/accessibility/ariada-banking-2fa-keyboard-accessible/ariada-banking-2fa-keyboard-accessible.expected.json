{
  "domain": "accessibility",
  "rule": "ariada/banking/2fa-keyboard-accessible",
  "standard": ["WCAG 2.2 SC 2.1.1", "EN 301 549 9.2.1.1", "EAA Annex I §I.4"],
  "cases": [
    {
      "id": "fail-1",
      "selector": "#fail-1 input[maxlength='1']",
      "expect": "fail",
      "severity": "critical",
      "why": "6 sibling inputs with maxlength='1' (≥3) form a 2FA row; all have inputmode='none' blocking soft keyboard and paste"
    },
    {
      "id": "fail-2",
      "selector": "#fail-2 input[maxlength='1']",
      "expect": "fail",
      "severity": "critical",
      "why": "6 sibling inputs with maxlength='1' (≥3) form a 2FA row; all have tabindex='-1' removing them from the tab order"
    },
    {
      "id": "fail-3",
      "selector": "#fail-3 input[maxlength='1']",
      "expect": "fail",
      "severity": "critical",
      "why": "6 sibling inputs with maxlength='1' (≥3) form a 2FA row; all have readonly defeating their input purpose"
    },
    {
      "id": "pass-1",
      "selector": "#pass-1 input[maxlength='1']",
      "expect": "pass"
    },
    {
      "id": "pass-2",
      "selector": "#pass-2 input",
      "expect": "pass"
    }
  ],
  "alsoExpect": []
}
