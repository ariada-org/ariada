{
  "domain": "ai-readiness",
  "rule": "ai-readiness/robots-missing",
  "standard": [
    "llms.txt community specification (Answer.AI, September 2024) — robots.txt prerequisite"
  ],
  "cases": [
    {
      "id": "fail-1",
      "selector": ":root",
      "expect": "fail",
      "severity": "serious",
      "why": "originArtifacts.robotsTxt is empty — AI_ROBOTS_PRESENT set to false; the origin has no robots.txt so AI crawlers apply fallback defaults and may over-index or under-index the site"
    },
    {
      "id": "pass-1",
      "selector": "#pass-1",
      "expect": "pass"
    }
  ],
  "alsoExpect": ["ai-readiness/llmstxt-missing"]
}
