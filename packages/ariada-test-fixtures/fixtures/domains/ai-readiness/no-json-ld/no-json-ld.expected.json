{
  "domain": "ai-readiness",
  "rule": "ai-readiness/no-json-ld",
  "standard": ["Schema.org structured data specification — JSON-LD encoding"],
  "cases": [
    {
      "id": "fail-1",
      "selector": ":root",
      "expect": "fail",
      "severity": "minor",
      "why": "No <script type=\"application/ld+json\"> block present anywhere in the HTML — parseJsonLd() regex finds no match; AI_SD_JSON_LD_PRESENT=false triggers RULE_NO_JSON_LD at minor severity"
    },
    {
      "id": "fail-2",
      "selector": ":root",
      "expect": "fail",
      "severity": "minor",
      "why": "<script type=\"text/javascript\"> is not matched by the application/ld+json regex — the page still has AI_SD_JSON_LD_PRESENT=false; RULE_NO_JSON_LD fires"
    },
    {
      "id": "pass-1",
      "selector": "#pass-1",
      "expect": "pass"
    }
  ],
  "alsoExpect": ["ai-readiness/robots-missing", "ai-readiness/llmstxt-missing"]
}
