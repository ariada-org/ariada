{
  "domain": "security",
  "rule": "sec-insecure-cookie",
  "standard": ["GDPR Art. 32"],
  "cases": [
    {
      "id": "fail-1",
      "selector": "#fail-1",
      "expect": "fail",
      "severity": "serious",
      "why": "Cookie 'session_id' has secure=false and httpOnly=false; both missingSecure and missingHttpOnly are true; SEC_COOKIE_INSECURE_FLAGS feature is emitted under cookie scope with joinValue='session_id'"
    },
    {
      "id": "fail-2",
      "selector": "#fail-2",
      "expect": "fail",
      "severity": "serious",
      "why": "Cookie 'auth_token' has secure=false; missingSecure=true triggers SEC_COOKIE_INSECURE_FLAGS even though httpOnly=true"
    },
    {
      "id": "fail-3",
      "selector": "#fail-3",
      "expect": "fail",
      "severity": "serious",
      "why": "Cookie '_analytics' has httpOnly=false; missingHttpOnly=true triggers SEC_COOKIE_INSECURE_FLAGS even though secure=true"
    },
    {
      "id": "pass-1",
      "selector": "#pass-1",
      "expect": "pass"
    },
    {
      "id": "pass-2",
      "selector": "#pass-2",
      "expect": "pass"
    }
  ],
  "alsoExpect": []
}
