{
  "name": "@ariada-integrations/xd-ariada",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Adobe XD export adapter and plugin panel fixture for Ariada CLI scans.",
  "scripts": {
    "build": "npm run validate:manifest && npm run typecheck",
    "evidence": "node scripts/scan-export.mjs",
    "lint": "node --check src/adapter.mjs && node --check plugin/main.js && node --check scripts/scan-export.mjs && node --check scripts/validate-manifest.mjs",
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "validate:manifest": "node scripts/validate-manifest.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
