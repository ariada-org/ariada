[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ariada-django"
version = "0.1.0"
description = "Django management command adapter for the Ariada accessibility scanner CLI."
readme = "README.md"
requires-python = ">=3.9"
license = "EUPL-1.2"
authors = [
  { name = "Alexander Brichkin (Agonist Development AB)", email = "git@ariada.org" }
]
dependencies = [
  "Django>=4.2"
]
keywords = ["accessibility", "django", "wcag", "eaa", "ariada"]

[project.optional-dependencies]
dev = [
  "build>=1.2",
  "pytest>=8.2",
  "ruff>=0.8"
]

[tool.setuptools.packages.find]
include = ["ariada_django*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
