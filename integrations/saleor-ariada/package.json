{
  "name": "@ariada-integrations/saleor-ariada",
  "version": "0.1.0",
  "private": true,
  "description": "A Saleor app that scans the connected storefront with the Ariada scanner and shows the findings in the dashboard.",
  "license": "EUPL-1.2",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/server.js",
    "test": "node --test \"test/*.test.mjs\""
  },
  "peerDependencies": {
    "@ariada-org/cli": ">=0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
