# Changelog

## 0.1.0

- Add shell-safe package scan glue for `lerna run a11y`.
- Add topology-aware canonical report aggregation and semantic exits.
- Add deterministic bundled package, offline consumer and real Lerna acceptance.
