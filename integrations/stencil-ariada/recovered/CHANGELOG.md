# Changelog

## 0.1.0 - 2026-07-14

- Add the Stencil `OutputTargetCustom` factory and per-component usage harnesses.
- Run real Ariada CLI/core-playwright/rules-axe scans over rendered shadow DOM.
- Add strict reports, severity build gating, loopback serving, and CLI support.
- Add exact bundled dependency closure, clean/offline consumer gates, deterministic
  package checksums/inventory, security checks, CI, docs, and a real Stencil fixture.
