{
  "name": "@ariada-integrations/web-ide-ariada",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test test/*.test.mjs",
    "validate": "node scripts/validate-templates.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
