// Client-rendered SPA (Cloudflare Pages static). No per-route prerender; the
// adapter emits an index.html fallback that boots the app on any path.
export const ssr = false;
export const prerender = false;
