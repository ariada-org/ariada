{
  "name": "ariada-wiki",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "build": "node scripts/generate.mjs",
    "check": "node scripts/generate.mjs --check",
    "test": "node --test tests/*.test.mjs",
    "deploy": "wrangler pages deploy dist --project-name=ariada-wiki"
  },
  "dependencies": {
    "@agonist/localization": "0.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
